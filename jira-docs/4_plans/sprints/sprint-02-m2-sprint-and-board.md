---
title: "Sprint 02 — M1 마감과 M2 스프린트·보드"
status: ready
milestone: M2
scope_count: 10
scope_points: 40
carried_over: 0
created: 2026-07-29
updated: 2026-07-29
---

# Sprint 02 — M1 마감과 M2 스프린트·보드

## 진행 현황 (2026-07-29)

| Wave | 스토리 | 점수 | 상태 |
|---|---|---:|---|
| 1 | r11b · r21 · r26 · r04b | 14 | ⬜ |
| 2 | r05a · r06 · r05b | 13 | ⬜ |
| 3 | r07a · r07b | 11 | ⬜ |
| 4 | r25 | 2 | ⬜ |

**0 / 40점 (0%)** · Sprint 01은 76/76점으로 마감했다.

## 1. 목표

두 가지를 한 스프린트에 담는다.

1. **M1을 실제로 끝낸다.** Sprint 01은 커밋 범위 76점을 다 했지만, M1 스토리 4건이 추정 시점에
   Wave 5로 밀려 남아 있다. 남겨 두면 "M1 완료"라고 말할 수 없다.
2. **보드에서 쓰기가 가능해진다.** 지금 화면은 읽기 전용이라 상태를 바꾸려면 CLI를 써야 한다.
   Wave 3이 끝나면 사람이 카드를 끌어 상태를 바꿀 수 있고, **이 도구로 이 도구의 작업을
   추적하는 것**(자기 관리)이 그 시점부터 가능해진다.

## 2. 이번 스프린트의 성격 — Sprint 01과 다른 점

Sprint 01은 76점 중 56점이 저장 코어였다. 파일·인덱스·크래시 복구처럼 **틀리면 조용히
데이터가 상하는** 영역이라 property test와 fault injection이 대부분의 노력을 가져갔다.

Sprint 02는 반대다. 40점 중 **21점이 화면**(r11b 2 · r07a 3 · r07b 8 · r25 2 · r04b의 UI 지분)이고,
나머지도 이미 있는 저장 계층 위에 얹는 도메인 규칙이다. 그래서 검증 방식이 달라진다:

- 저장 계층은 **테스트로** 증명했다. 화면은 테스트만으로는 부족하다 — Sprint 01에서 UI를
  실제로 브라우저에서 열어 보고서야 배지가 제대로 붙는지 확인할 수 있었다.
- 따라서 각 UI 스토리는 **브라우저 확인을 완료 조건에 포함**한다. 접근성 스냅샷으로 구조를
  확인하고, 색으로만 구분되는 정보가 없는지 본다(r14b에서 정한 규칙).

> **경고 — 현재 UI 스택.** §8은 Next.js App Router + shadcn/ui를 전제로 쓰였지만 실제로는
> 의존성 0개의 바닐라 화면이다. r07b(드래그 8점)는 그 위에서 드래그·드롭을 직접 구현한다는
> 뜻이다. 스택을 바꿀지 여부는 Wave 3 착수 전에 결정해야 하며, 바꾼다면 **의존성 0개라는
> 제약을 깨는 결정**이므로 ADR이 필요하다. → §5 OQ-S2-1

## 3. 웨이브와 게이트

### Wave 1 — M1 마감 (14점)

| 순서 | 스토리 | 점수 | 선행 | 내용 |
|---|---|---:|---|---|
| 1.1 | [r11b](../../2_requirements/story/r11b-integrity-error-banner.md) | 2 | r11a ✅ | 무결성 오류 배너·격리 항목 화면 |
| 1.2 | [r21](../../2_requirements/story/r21-manual-reindex-full-verify.md) | 2 | r08a ✅ r11a ✅ | 수동 전체 리인덱스·전체 검증 |
| 1.3 | [r26](../../2_requirements/story/r26-auto-rekey-former-keys.md) | 5 | r08c ✅ r11a ✅ | 표시 키 자동 재키잉·`former_keys` alias |
| 1.4 | [r04b](../../2_requirements/story/r04b-fulltext-search.md) | 5 | r04a ✅ | FTS5 전문 검색·키 alias 조회 |

**Gate 1** — M1이 문서상으로도 실제로도 끝난다.
- 격리 항목이 화면에 뜨고, 사람이 파일을 고치면 배너가 사라진다
- 중복 표시 키가 자동 재키잉되고 이전 키로도 조회된다 (r11a가 "격리하지 않고 넘긴다"까지만
  해 둔 경로의 반대편)
- 5,000건에서 전문 검색이 N1 예산 안에 든다

### Wave 2 — 스프린트 도메인 (13점)

| 순서 | 스토리 | 점수 | 선행 | 내용 |
|---|---|---:|---|---|
| 2.1 | [r05a](../../2_requirements/story/r05a-sprint-crud.md) | 3 | r10 ✅ | 스프린트 CRUD (파일 SoT) |
| 2.2 | [r06](../../2_requirements/story/r06-sprint-planning-capacity.md) | 5 | r05a · r03 ✅ · r04a ✅ | 백로그↔스프린트 이동·capacity 경고 |
| 2.3 | [r05b](../../2_requirements/story/r05b-sprint-start-close-carryover.md) | 5 | r05a · r06 | 시작·종료·미완료 이월 |

r06을 r05b보다 앞에 두는 이유는 스토리에 적힌 그대로다 — **capacity 경고 산출식을 둘이
공유**하므로, 종료·이월을 먼저 만들면 같은 식을 두 번 쓰게 된다.

**Gate 2** — 스프린트를 운영할 수 있다.
- 스프린트를 만들고 이슈를 담고 시작·종료할 수 있다
- **ACTIVE 유일성이 강제된다** — r11a가 충돌을 감지까지만 하고 차단할 명령이 없어 이월한
  인수조건이 여기서 닫힌다
- 종료 시 미완료 이슈가 이월되고 `backlog_rank`는 보존된다 (ADR-005 §1)

### Wave 3 — 칸반 보드 (11점)

| 순서 | 스토리 | 점수 | 선행 | 내용 |
|---|---|---:|---|---|
| 3.1 | [r07a](../../2_requirements/story/r07a-board-columns-cards.md) | 3 | r05b · r06 · r14a ✅ | 상태 컬럼·카드 배지 |
| 3.2 | [r07b](../../2_requirements/story/r07b-board-drag-transition-rank.md) | 8 | r07a · r01b ✅ · r03 ✅ | 드래그 — 전이표 준수·`board_rank` 유지 |

**Gate 3 — 이 스프린트의 분수령.** 여기를 넘으면 화면에서 쓰기가 된다.
- 카드를 끌어 상태를 바꿀 수 있고, **§5.2 전이표에 없는 이동은 화면에서 거부**된다
  (서버가 400을 주기 전에 UI가 먼저 막아야 드래그가 되돌아오는 경험이 생기지 않는다)
- 같은 컬럼 안에서 끌면 `board_rank`만 바뀌고 `backlog_rank`는 그대로다
- 드래그 중 남이 같은 이슈를 바꾸면 412를 받고 화면이 현재 상태로 되돌아온다

> **자기 관리(우로보로스)는 Gate 3 이후.** 지금도 CLI로 이슈를 만들어 추적할 수는 있지만,
> 상태 변경까지 화면에서 되어야 실제로 쓰게 된다. Gate 3 직후 남은 26건을 보드에 적재한다.

### Wave 4 — git 상태 배지 (2점)

| 순서 | 스토리 | 점수 | 선행 | 내용 |
|---|---|---:|---|---|
| 4.1 | [r25](../../2_requirements/story/r25-git-status-badge.md) | 2 | M0 ✅ | 미커밋 건수·마지막 push 시각 |

D14가 커밋↔이슈 연결을 인덱스 전용으로 못박은 이유가 여기서 눈에 보인다 — 이슈 파일에
커밋 정보를 쓰면 **이 배지가 상시 증가**한다.

## 4. 추정

척도는 Sprint 01과 같은 스토리 포인트(D8)다. Wave 1의 4건은 Sprint 01 추정표의 값을 그대로
가져왔고, M2 6건이 이번에 새로 매긴 값이다.

| 스토리 | 점수 | 근거 |
|---|---:|---|
| r07b 보드 드래그 | **8** | 의존성 0개 스택에서 드래그·드롭을 직접 구현한다. 전이표 사전 검증 + `board_rank` 계산 + 낙관적 동시성 롤백까지 셋이 겹친다 |
| r26 자동 재키잉 | **5** | 결정성이 핵심. 어느 클론에서 돌려도 같은 키가 나와야 하므로 property test가 필요하다 |
| r04b 전문 검색 | **5** | FTS5 + 토크나이저 실측(OQ1) + `former_keys` alias 해석 |
| r06 스프린트 계획 | **5** | 백로그↔스프린트 이동이 rank 두 개를 동시에 건드리고, capacity 산출식이 r05b와 공유된다 |
| r05b 시작·종료·이월 | **5** | 상태 기계 + ACTIVE 유일성 강제 + 미완료 이월. 이월이 다건 쓰기다 |
| r05a 스프린트 CRUD | **3** | ETag·검증 계약이 이미 있어 얇다 |
| r07a 보드 컬럼·카드 | **3** | 현재 화면이 이미 상태별 컬럼을 그린다. 스프린트 범위와 배지를 얹는 정도 |
| r11b 오류 배너 | **2** | r11a가 `GET /integrity/issues`까지 만들어 뒀다 |
| r21 수동 리인덱스 | **2** | 세대 교체는 r08a에 있다. 명령과 보고만 |
| r25 git 상태 배지 | **2** | `git status --porcelain` 집계와 마지막 push 시각 |
| **합계** | **40** | |

> **배분을 보라** — 화면이 15점(r07a 3 · r07b 8 · r11b 2 · r25 2)으로 38%다. Sprint 01에서
> 저장 코어가 62%였던 것과 정확히 반대다. **r07b 하나가 8점으로 전체의 20%**이며, 이 스프린트가
> 막힌다면 십중팔구 거기다.

## 5. 착수 전 결정해야 할 것

| # | 질문 | 왜 지금 | 기한 |
|---|---|---|---|
| **OQ-S2-1** | 화면 스택을 유지할지(의존성 0개 바닐라) §8대로 Next.js+shadcn으로 갈지 | r07b 추정이 스택에 따라 8점에서 크게 움직인다. 바꾸면 **의존성 0개 제약을 깨는 결정**이라 ADR이 필요하다 | Wave 3 착수 전 |
| **OQ-S2-2** | 스프린트 종료 시 미완료 이슈의 기본 처리 — 다음 스프린트로 이월인지 백로그 복귀인지 | r05b 인수조건이 갈린다 | Wave 2 착수 전 |
| **OQ-S2-3** | capacity 초과를 경고로만 둘지 시작을 막을지 | r06·r05b가 같은 식을 공유한다 | Wave 2 착수 전 |

세 건 다 **계약을 바꾸므로** 해당 웨이브 착수 전에 닫는다. Sprint 01에서 배운 것 —
검증 불가능한 인수조건을 열어 두면 스프린트가 끝날 때까지 아무도 모른다.

## 6. 이번 스프린트에 적용하는 규칙

Sprint 01에서 손해를 보고 세운 것들이다.

- **브랜치는 `feat/<스토리>`, 반영은 PR.** Sprint 01 중반까지 develop에 직접 푸시했고 그것이
  팀 규칙 위반이었다. r14b부터 전환했다.
- **`npm run docs:check`가 CI 필수 단계다.** 스프린트 문서의 숫자·완료 표시는 스토리 파일에서
  파생되며, 어긋나면 빌드가 깨진다. 이제 **모든** 스프린트 문서를 검사한다.
- **테스트는 병렬로 돌린다.** 직렬화는 Linux 락 결함을 가렸다. 병렬 실행 자체가 재발 감시다.
- **검증 명령에 파이프를 붙이지 않는다.** 종료 코드가 파이프 마지막 명령의 것으로 바뀐다.
- **스토리를 열 때 인수조건이 이번 마일스톤에서 검증 가능한지 본다.** Sprint 01에서 78건 중
  14건이 애초에 닫을 수 없는 상태였고, 그건 구현 실패가 아니라 분해 실수였다.
- **UI 스토리는 브라우저 확인을 완료 조건에 포함한다.**

## 7. 범위 밖

- M3 이후 전부(에이전트 토큰·claim·run·코멘트) — 26건 중 16건이 여기에 있다
- 화면 스택 교체 자체 — OQ-S2-1에서 결정하되, 교체한다면 별도 스토리로 연다
- 재균형의 다중 파일 원자성 — r03이 남긴 격차. outbox가 여러 경로를 담아야 하므로 r09의
  복구 기계를 건드린다. 별도 스토리가 필요하다
- 자기 관리(우로보로스) 적재 — Gate 3 이후 작업이며 스토리가 아니다

## 8. 참고

- 이전 스프린트: [sprint-01](sprint-01-m1-reliable-core.md) · [결정 기록](sprint-01-decisions.md)
- PRD `../../2_requirements/prd/backlog-sprint.md` §5.2(전이표) · §7 R5·R6·R7 · §8(IA)
- ADR `../../0_decisions/adr-005-lexorank-ordering.md`(두 rank의 독립성)
